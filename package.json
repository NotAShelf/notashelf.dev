{
  "private": true,
  "workspaces": ["apps/web", "packages/wasm/pkg"],
  "scripts": {
    "build": "pnpm run build:wasm && pnpm --filter ./apps/web run build",
    "build:wasm": "cd packages/wasm && wasm-pack build --target web --out-dir pkg --out-name wasm-utils",
    "dev": "pnpm run build:wasm && pnpm --filter ./apps/web run dev",
    "preview": "pnpm --filter ./apps/web run preview",
    "check": "pnpm --filter ./apps/web run check",
    "fmt": "pnpm --filter ./apps/web run fmt"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}