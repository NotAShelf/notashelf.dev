---
interface Props {
  href: string;
  external?: boolean;
  class?: string;
  underline?: boolean;
  [key: string]: any;
}

const { href, external, ...rest } = Astro.props;
---

<a href={href} target={external ? "_blank" : "_self"} {...rest}>
  <slot />
</a>
